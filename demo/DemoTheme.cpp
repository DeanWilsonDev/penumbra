#include "DemoTheme.h"

// Theme is currently a plain value type with in-class defaults, so it needs no
// out-of-line definitions yet. This translation unit exists as the home for
// future non-inline theme logic (e.g. loading values from disk) and to keep the
// project layout stable as resolvers are added in later milestones.
